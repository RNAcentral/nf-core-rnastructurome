{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/nf-core/rnastructurome/master/nextflow_schema.json",
    "title": "nf-core/rnastructurome pipeline parameters",
    "description": "This pipeline analyses chemical-based high-throughput RNA structure probing data",
    "type": "object",
    "$defs": {
        "input_output_options": {
            "title": "Input/output options",
            "type": "object",
            "fa_icon": "fas fa-terminal",
            "description": "Define where the pipeline should find input data and save output data.",
            "required": ["input", "outdir"],
            "properties": {
                "input": {
                    "type": "string",
                    "format": "file-path",
                    "exists": true,
                    "schema": "assets/schema_input.json",
                    "mimetype": "text/csv",
                    "pattern": "^\\S+\\.csv$",
                    "description": "Path to comma-separated file containing information about the samples in the experiment.",
                    "help_text": "You will need to create a design file with information about the samples in your experiment before running the pipeline. Use this parameter to specify its location. At minimum it must include `sample`, `fastq_1`, `cell_line`, `condition`, and `replicate` columns, with optional `fastq_2` for paired-end data. `sample_id`, `method`, `principle`, `organism`, `adapter_3p`, `adapter_5p`, and `umi_pattern` can be optionally specified in the samplesheet or as flags.",
                    "fa_icon": "fas fa-file-csv"
                },
                "outdir": {
                    "type": "string",
                    "format": "directory-path",
                    "description": "The output directory where the results will be saved. You have to use absolute paths to storage on Cloud infrastructure.",
                    "fa_icon": "fas fa-folder-open"
                },
                "email": {
                    "type": "string",
                    "description": "Email address for completion summary.",
                    "fa_icon": "fas fa-envelope",
                    "help_text": "Set this parameter to your e-mail address to get a summary e-mail with details of the run sent to you when the workflow exits. If set in your user config file (`~/.nextflow/config`) then you don't need to specify this on the command line for every run.",
                    "pattern": "^([a-zA-Z0-9_\\-\\.]+)@([a-zA-Z0-9_\\-\\.]+)\\.([a-zA-Z]{2,5})$"
                },
                "multiqc_title": {
                    "type": "string",
                    "description": "MultiQC report title. Printed as page header, used for filename if not otherwise specified.",
                    "fa_icon": "fas fa-file-signature"
                }
            }
        },
        "reference_genome_options": {
            "title": "Reference genome options",
            "type": "object",
            "fa_icon": "fas fa-dna",
            "description": "Reference genome related files and options required for the workflow.",
            "properties": {
                "fasta": {
                    "type": "string",
                    "format": "file-path",
                    "exists": true,
                    "mimetype": "text/plain",
                    "pattern": "^\\S+\\.fn?a(sta)?(\\.gz)?$",
                    "description": "Path to transcript FASTA file.",
                    "help_text": "Provide a transcript-level FASTA for rf-count/rf-norm/rf-fold. If omitted, the pipeline will try to resolve transcript FASTA from `params.genomes[organism]` (`transcript_fasta`, `transcriptome`, or `cdna`).",
                    "fa_icon": "far fa-file-code"
                },
                "gtf": {
                    "type": "string",
                    "format": "file-path",
                    "exists": true,
                    "mimetype": "text/plain",
                    "pattern": "^\\S+\\.gtf(\\.gz)?$",
                    "description": "Path to transcript annotation GTF file.",
                    "help_text": "Provide a GTF annotation directly. If omitted, the pipeline will try to resolve GTF from `params.genomes[organism].gtf` or download one from Ensembl.",
                    "fa_icon": "far fa-file-alt"
                },
                "genomes": {
                    "type": "object",
                    "description": "Optional custom reference map keyed by organism/reference name.",
                    "fa_icon": "fas fa-ban",
                    "hidden": true,
                    "additionalProperties": true
                },
                "ensembl_base_url": {
                    "type": "string",
                    "description": "Ensembl FTP base URL used when auto-resolving transcript FASTA.",
                    "default": "https://ftp.ensembl.org/pub"
                },
                "ensembl_release": {
                    "type": "string",
                    "description": "Ensembl release for transcript FASTA auto-resolution (`current`, `latest`, or release number like `114`).",
                    "default": "current"
                },
                "ensembl_species_map": {
                    "type": "object",
                    "description": "Optional map from organism keys to Ensembl species names used for transcript FASTA auto-resolution.",
                    "hidden": true,
                    "additionalProperties": true
                },
                "ncbi_accessions_map": {
                    "type": "object",
                    "description": "Internal map from normalized viral organism keys to NCBI accession lists.",
                    "hidden": true,
                    "additionalProperties": {
                        "type": "array",
                        "items": {
                            "type": "string"
                        }
                    }
                }
            }
        },
        "sample_metadata_options": {
            "title": "Sample metadata options",
            "type": "object",
            "fa_icon": "fas fa-tags",
            "description": "Global fallback metadata values used when the corresponding column is missing or empty in the input CSV.",
            "properties": {
                "sample_id": {
                    "type": "string",
                    "description": "Fallback sample ID for rows missing `sample_id`.",
                    "pattern": "^\\S+$"
                },
                "method": {
                    "type": "string",
                    "description": "Fallback probing method for rows missing `method`."
                },
                "principle": {
                    "type": "string",
                    "description": "Fallback probing principle for rows missing `principle`.",
                    "enum": ["RT-stop", "MaP"]
                },
                "organism": {
                    "type": "string",
                    "description": "Fallback organism for rows missing `organism`, typically a Latin binomial such as `Homo sapiens`."
                },
                "umi_pattern": {
                    "type": "string",
                    "description": "Fallback UMI pattern for rows missing `umi_pattern`. Supplying a pattern enables umi_tools extract before cutadapt."
                }
            }
        },
        "read_trimming_options": {
            "title": "Read trimming options",
            "type": "object",
            "fa_icon": "fas fa-cut",
            "description": "Cutadapt trimming parameters.",
            "properties": {
                "cutadapt_adapter_5p": {
                    "type": "string",
                    "description": "Global fallback 5' adapter sequence(s), comma-separated. If omitted and no per-sample adapter is provided, the pipeline defaults to AGATCGGAAGAGC."
                },
                "cutadapt_adapter_3p": {
                    "type": "string",
                    "description": "Global fallback 3' adapter sequence(s), comma-separated. If omitted and no per-sample adapter is provided, the pipeline defaults to AGATCGGAAGAGC."
                },
                "cutadapt_quality_only": {
                    "type": "boolean",
                    "description": "If true, skip adapter trimming and apply quality/length trimming only.",
                    "default": false
                },
                "cutadapt_5quality": {
                    "type": "integer",
                    "description": "Base quality threshold for 5' trimming (used for MaP; RT-stop is forced to 0).",
                    "default": 20
                },
                "cutadapt_3quality": {
                    "type": "integer",
                    "description": "Base quality threshold for 3' trimming.",
                    "default": 20
                },
                "cutadapt_len": {
                    "type": "integer",
                    "description": "Minimum read length retained after trimming.",
                    "default": 25
                },
                "cutadapt_min_align": {
                    "type": "integer",
                    "description": "Minimum adapter overlap required for trimming.",
                    "default": 10
                },
                "cutadapt_trim_n": {
                    "type": "boolean",
                    "description": "Trim terminal N bases.",
                    "default": true
                }
            }
        },
        "alignment_options": {
            "title": "Alignment options",
            "type": "object",
            "fa_icon": "fas fa-map",
            "description": "STAR/Bowtie/Bowtie2 mapping options.",
            "properties": {
                "transcriptome": {
                    "type": "boolean",
                    "description": "Align against a transcript-level FASTA using Bowtie (RT-stop) / Bowtie2 (MaP). Default (false) downloads a genome FASTA and aligns with STAR.",
                    "default": false,
                    "fa_icon": "fas fa-sitemap"
                },
                "bowtie_mapping_params": {
                    "type": "string",
                    "description": "Additional aligner options appended to Bowtie/Bowtie2 commands."
                },
                "bowtie_manual_only": {
                    "type": "boolean",
                    "description": "Use only options from `--bowtie_mapping_params` and ignore other Bowtie/Bowtie2 tuning flags.",
                    "default": false
                },
                "bowtie_k": {
                    "type": "integer",
                    "minimum": 1,
                    "description": "Report up to this number of alignments per read (`-k`)."
                },
                "bowtie_all": {
                    "type": ["boolean", "string"],
                    "description": "Report all valid alignments (`-a`).",
                    "default": false
                },
                "bowtie_norc": {
                    "type": "boolean",
                    "description": "Map only to sense strand (`--norc`).",
                    "default": false
                },
                "bowtie_trim5": {
                    "type": "integer",
                    "minimum": 0,
                    "description": "Trim this many bases from 5' read end (`--trim5`).",
                    "default": 0
                },
                "bowtie_trim3": {
                    "type": "integer",
                    "minimum": 0,
                    "description": "Trim this many bases from 3' read end (`--trim3`).",
                    "default": 0
                },
                "bowtie_index": {
                    "type": "string",
                    "description": "Optional path to a prebuilt Bowtie index (currently not consumed by workflow logic)."
                },
                "bowtie_seedlen": {
                    "type": "integer",
                    "minimum": 3,
                    "description": "Seed length (`-l`/`-L`) used by Bowtie/Bowtie2."
                },
                "bowtie_n": {
                    "type": "integer",
                    "minimum": 0,
                    "maximum": 3,
                    "description": "Bowtie v1 `-n` seed mismatch mode value.",
                    "default": 2
                },
                "bowtie_v": {
                    "type": "integer",
                    "minimum": 0,
                    "maximum": 3,
                    "description": "Bowtie v1 `-v` mismatch mode value."
                },
                "bowtie_max": {
                    "type": "integer",
                    "minimum": 1,
                    "description": "Bowtie v1 `-m` maximum reportable alignments per read before suppression.",
                    "default": 1
                },
                "bowtie_chunkmbs": {
                    "type": "integer",
                    "minimum": 1,
                    "description": "Bowtie v1 `--chunkmbs` memory chunk size.",
                    "default": 512
                },
                "bowtie2_N": {
                    "type": "integer",
                    "minimum": 0,
                    "maximum": 1,
                    "description": "Bowtie2 `-N` seed mismatches.",
                    "default": 0
                },
                "bowtie2_D": {
                    "type": "integer",
                    "minimum": 0,
                    "description": "Bowtie2 `-D` max seed extension attempts.",
                    "default": 15
                },
                "bowtie2_R": {
                    "type": "integer",
                    "minimum": 0,
                    "description": "Bowtie2 `-R` max re-seeding attempts.",
                    "default": 2
                },
                "bowtie2_mp": {
                    "type": "string",
                    "pattern": "^\\d+,\\d+$",
                    "description": "Bowtie2 `--mp` mismatch penalties as `max,min`.",
                    "default": "6,2"
                },
                "bowtie2_dpad": {
                    "type": "integer",
                    "minimum": 0,
                    "description": "Bowtie2 `--dpad` padding around DP table.",
                    "default": 15
                },
                "bowtie2_rdg": {
                    "type": "string",
                    "pattern": "^\\d+,\\d+$",
                    "description": "Bowtie2 `--rdg` read gap penalties as `open,extend`.",
                    "default": "5,3"
                },
                "bowtie2_rfg": {
                    "type": "string",
                    "pattern": "^\\d+,\\d+$",
                    "description": "Bowtie2 `--rfg` reference gap penalties as `open,extend`.",
                    "default": "5,3"
                },
                "bowtie2_softclip": {
                    "type": "boolean",
                    "description": "Enable Bowtie2 local alignment mode (`--local`).",
                    "default": false
                },
                "bowtie2_ma": {
                    "type": "integer",
                    "description": "Bowtie2 `--ma` match bonus used with `--local`.",
                    "default": 2
                },
                "bowtie2_dovetail": {
                    "type": "boolean",
                    "description": "Allow dovetailing mates (`--dovetail`).",
                    "default": false
                },
                "star_map_sjdb_overhang": {
                    "type": "integer",
                    "minimum": 1,
                    "description": "STAR `--sjdbOverhang` for MaP alignment. Ideally `readLength - 1`; default 200 suits 201 bp reads.",
                    "default": 200
                }
            }
        },
        "rnaframework_options": {
            "title": "RNAframework options",
            "type": "object",
            "fa_icon": "fas fa-wave-square",
            "description": "rf-count/rf-norm/rf-fold options.",
            "allOf": [
                {
                    "not": {
                        "properties": {
                            "rfcount_paired_only": {
                                "const": true
                            },
                            "rfcount_properly_paired": {
                                "const": true
                            }
                        },
                        "required": ["rfcount_paired_only", "rfcount_properly_paired"]
                    }
                }
            ],
            "properties": {
                "rfcount_img": {
                    "type": "boolean",
                    "description": "Deprecated: rf-count plot generation (`-g`) is always enabled.",
                    "default": true
                },
                "rfcount_strandedness": {
                    "type": "string",
                    "enum": ["unstranded", "first", "second"],
                    "description": "Library strandedness fallback for rf-count-genome when RSeQC inference is unavailable (e.g. viral/bacterial references without a usable BED annotation).",
                    "default": "unstranded"
                },
                "rfcount_genome_map_quality": {
                    "type": "integer",
                    "minimum": 0,
                    "description": "Minimum MAPQ score for rf-count-genome (`--map-quality`). Useful for filtering multi-mappers; 0 keeps all alignments."
                },
                "rfcount_genome_block_size": {
                    "type": "integer",
                    "minimum": 1,
                    "description": "Block size for per-chromosome memory allocation in rf-count-genome (`-bs`); increase for large genomes.",
                    "default": 100000
                },
                "rfcount_genome_samtools_threads": {
                    "type": "integer",
                    "minimum": 1,
                    "description": "SAMtools working threads per processor instance in rf-count-genome (`-wt`); total threads = `-p` × `-wt`.",
                    "default": 1
                },
                "rfcount_map_max_edit_distance": {
                    "type": "number",
                    "exclusiveMinimum": 0,
                    "maximum": 1,
                    "description": "MaP only: discard reads whose edit-distance frequency exceeds this threshold (`-me`), value in range (0, 1].",
                    "default": 0.15
                },
                "rfcount_map_median_quality": {
                    "type": "integer",
                    "minimum": 0,
                    "description": "MaP only: discard reads with median Phred+33 base quality below this value (`-eq`).",
                    "default": 20
                },
                "rfcount_map_no_deletions": {
                    "type": "boolean",
                    "description": "MaP only: ignore deletion events during mutation counting (`-nd`).",
                    "default": false
                },
                "rfcount_map_no_insertions": {
                    "type": "boolean",
                    "description": "MaP only: ignore insertion events during mutation counting (`-ni`).",
                    "default": false
                },
                "rfcount_map_eval_surrounding": {
                    "type": "boolean",
                    "description": "MaP only: also evaluate quality of ±1 nt surrounding each mutation (`-es`).",
                    "default": false
                },
                "rfcount_trim_5prime": {
                    "type": "integer",
                    "minimum": 0,
                    "description": "Trim this many bases from the 5' read end for rf-count (`-t5`).",
                    "default": 0
                },
                "rfcount_mask_file": {
                    "type": "string",
                    "format": "file-path",
                    "exists": true,
                    "description": "Path to rf-count mask file (`--mask-file`)."
                },
                "rfcount_primary_only": {
                    "type": "boolean",
                    "description": "Use only primary alignments in rf-count (`--primary-only`).",
                    "default": false
                },
                "rfcount_paired_only": {
                    "type": "boolean",
                    "description": "For paired-end samples, use only reads with both mates mapped (`--paired-only`).",
                    "default": false
                },
                "rfcount_properly_paired": {
                    "type": "boolean",
                    "description": "For paired-end samples, use only properly paired reads (`--properly-paired`).",
                    "default": false
                },
                "rfcount_map_sort_by_read_name": {
                    "type": "boolean",
                    "description": "In mutation mode (`-m`), pre-sort paired-end reads by read name (`--sort-by-read-name`).",
                    "default": false
                },
                "rfcount_map_discard_shorter": {
                    "type": "integer",
                    "minimum": 1,
                    "description": "In mutation mode (`-m`), discard reads shorter than this value (`--discard-shorter`).",
                    "default": 1
                },
                "rfcount_map_min_quality": {
                    "type": "integer",
                    "minimum": 0,
                    "description": "In mutation mode (`-m`), minimum Phred+33 base quality for mutations (`--min-quality`).",
                    "default": 20
                },
                "rfcount_map_collapse_consecutive": {
                    "type": "boolean",
                    "description": "In mutation mode (`-m`), collapse consecutive mutations/indels (`--collapse-consecutive`).",
                    "default": true
                },
                "rfcount_map_max_collapse_distance": {
                    "type": "integer",
                    "minimum": 0,
                    "description": "In mutation mode with collapsing enabled, max distance to collapse (`--max-collapse-distance`).",
                    "default": 2
                },
                "rfnorm_remap_reactivities": {
                    "type": "boolean",
                    "description": "Remap normalized reactivities to the 0-1 range according to Zarringhalam et al. (`--remap-reactivities`).",
                    "default": false
                },
                "rfnorm_reactive_bases": {
                    "type": "string",
                    "pattern": "^[A-Za-z]+$",
                    "description": "Reactive bases used for normalization window selection (`--reactive-bases`), e.g. `AC` for DMS."
                },
                "rfnorm_norm_window": {
                    "type": "integer",
                    "minimum": 1,
                    "description": "Normalization window size (`--norm-window`)."
                },
                "rfnorm_window_offset": {
                    "type": "integer",
                    "minimum": 0,
                    "description": "Normalization window offset (`--window-offset`)."
                },
                "rfnorm_dynamic_window": {
                    "type": "integer",
                    "minimum": 1,
                    "description": "Dynamically resize normalization windows to include at least this many reactive bases (`--dynamic-window`)."
                },
                "rfnorm_norm_independent": {
                    "type": "boolean",
                    "description": "Normalize each reactive base independently (`--norm-independent`).",
                    "default": false
                },
                "rfnorm_norm_factor": {
                    "type": "string",
                    "description": "Use a fixed normalization factor for all transcripts (`--norm-factor`). For 90% Winsorizing provide two comma-separated values for the 5th and 95th percentiles."
                },
                "rfnorm_norm_method": {
                    "type": "integer",
                    "enum": [2, 3],
                    "description": "Override rf-norm normalization method (`-nm`)."
                },
                "rfnorm_raw": {
                    "type": "boolean",
                    "description": "Score raw reactivities without applying normalization (`--raw`).",
                    "default": false
                },
                "rfnorm_pseudocount": {
                    "type": "number",
                    "minimum": 0,
                    "description": "Pseudocount used by Ding scoring (`--pseudocount`)."
                },
                "rfnorm_max_score": {
                    "type": "number",
                    "minimum": 0,
                    "description": "Maximum score used by Ding scoring (`--max-score`)."
                },
                "rfnorm_ignore_lower_than_untreated": {
                    "type": "boolean",
                    "description": "Set reactivities lower than untreated to zero for Ding or Siegfried scoring (`--ignore-lower-than-untreated`).",
                    "default": false
                },
                "rfnorm_max_untreated_mut": {
                    "type": "number",
                    "minimum": 0,
                    "description": "Maximum untreated mutation rate threshold for Siegfried scoring (`--max-untreated-mut`)."
                },
                "rfnorm_max_mutation_rate": {
                    "type": "number",
                    "minimum": 0,
                    "description": "Maximum mutation rate threshold for MaP scoring methods (`--max-mutation-rate`)."
                },
                "rfnorm_mean_coverage": {
                    "type": "number",
                    "minimum": 0,
                    "description": "Discard transcripts with mean coverage below this threshold (`--mean-coverage`).",
                    "default": 10
                },
                "rfnorm_median_coverage": {
                    "type": "number",
                    "minimum": 0,
                    "description": "Discard transcripts with median coverage below this threshold (`--median-coverage`).",
                    "default": 0
                },
                "rfnorm_nan": {
                    "type": "integer",
                    "minimum": 0,
                    "description": "Positions with read coverage below this threshold are reported as NaN (`--nan`). Set to 0 to disable NaN masking.",
                    "default": 1000
                },
                "rfnorm_img": {
                    "type": "boolean",
                    "description": "Deprecated: rf-norm plot generation (`--img`) is always enabled.",
                    "default": true
                },
                "vienna_structures": {
                    "type": "boolean",
                    "description": "Generate ViennaRNA RNAplot structure diagrams via rf-fold (`-g`). Disabled by default; use `--r2dt` for template-based diagrams instead.",
                    "default": false
                },
                "jackknife_reference": {
                    "type": "string",
                    "format": "file-path",
                    "exists": true,
                    "description": "Path to a reference `.db` structure file for rf-jackknife normalisation assessment. When provided, rf-jackknife runs between rf-norm and rf-fold and the output CSV reports optimal slope/intercept values. When omitted, rf-fold runs directly using whatever slope/intercept params are set."
                },
                "rfjackknife_slope": {
                    "type": "string",
                    "pattern": "^-?\\d+(\\.\\d+)?,-?\\d+(\\.\\d+)?$",
                    "description": "Comma-separated slope range to search in rf-jackknife (`-sl`), e.g. `0,5`.",
                    "default": "0,5"
                },
                "rfjackknife_intercept": {
                    "type": "string",
                    "pattern": "^-?\\d+(\\.\\d+)?,-?\\d+(\\.\\d+)?$",
                    "description": "Comma-separated intercept range to search in rf-jackknife (`-in`), e.g. `-3,0`.",
                    "default": "-3,0"
                },
                "rfjackknife_slope_step": {
                    "type": "number",
                    "exclusiveMinimum": 0,
                    "description": "Step size for slope grid search in rf-jackknife (`-ss`).",
                    "default": 0.2
                },
                "rfjackknife_intercept_step": {
                    "type": "number",
                    "exclusiveMinimum": 0,
                    "description": "Step size for intercept grid search in rf-jackknife (`-is`).",
                    "default": 0.2
                },
                "rfjackknife_mfmi": {
                    "type": "boolean",
                    "description": "Use modified FMI (Lan et al. 2022) instead of standard FMI in rf-jackknife (`-m`).",
                    "default": false
                },
                "rfjackknife_only_common": {
                    "type": "boolean",
                    "description": "Only use transcripts present across all replicates in rf-jackknife (`-oc`).",
                    "default": false
                },
                "rfjackknife_img": {
                    "type": "boolean",
                    "description": "Generate R heatmap of grid-search results in rf-jackknife (`-g`).",
                    "default": false
                },
                "rfjackknife_rf_fold_params": {
                    "type": "string",
                    "description": "Additional rf-fold parameters passed inside rf-jackknife (`-rp`), e.g. `-md 500`."
                },
                "rfrctools_gtf_feature": {
                    "type": "string",
                    "description": "GTF feature type to extract with rf-rctools (`-f`); override for non-standard annotations.",
                    "default": "exon"
                },
                "rfrctools_gtf_attribute": {
                    "type": "string",
                    "description": "GTF attribute used as the output RC entry ID by rf-rctools (`-b`).",
                    "default": "transcript_id"
                },
                "rfeval_reactivity_cutoff": {
                    "type": "number",
                    "minimum": 0,
                    "maximum": 1,
                    "description": "Cutoff for classifying a base as highly-reactive in rf-eval unpaired-coefficient calculation (`-c`).",
                    "default": 0.7
                },
                "rfeval_ignore_terminal": {
                    "type": "boolean",
                    "description": "Exclude terminal base-pairs from rf-eval calculations (`-it`).",
                    "default": false
                },
                "rfeval_keep_pseudoknots": {
                    "type": "boolean",
                    "description": "Retain pseudoknotted base-pairs in rf-eval (`-kp`).",
                    "default": false
                },
                "rfeval_keep_lonelypairs": {
                    "type": "boolean",
                    "description": "Retain lonely/isolated base-pairs in rf-eval (`-kl`).",
                    "default": false
                },
                "rfeval_img": {
                    "type": "boolean",
                    "description": "Generate R metric plots in rf-eval (`-g`).",
                    "default": false
                },
                "rffold_ct": {
                    "type": "boolean",
                    "description": "Write CT format structures with rf-fold (`-ct`). Disabled by default to keep dot-bracket output.",
                    "default": false
                },
                "rffold_window": {
                    "type": "boolean",
                    "description": "Enable windowed folding in rf-fold (`-w`).",
                    "default": true
                },
                "rffold_unconstrained": {
                    "type": "boolean",
                    "description": "Fold without reactivity constraints in rf-fold (`-u`).",
                    "default": false
                },
                "rffold_vienna_no_lonely_pairs": {
                    "type": "boolean",
                    "description": "Pass ViennaRNA no-lonely-pairs mode to rf-fold (`-vnlp`).",
                    "default": false
                },
                "rffold_vienna_constrained": {
                    "type": "boolean",
                    "description": "Use ViennaRNA hard constraints with rf-fold (`-vc`).",
                    "default": false
                },
                "rffold_vienna_max_bp_span": {
                    "type": "integer",
                    "minimum": 1,
                    "description": "Maximal base-pair span for ViennaRNA in rf-fold (`-vmd`)."
                },
                "rffold_vienna_bp_span": {
                    "type": "integer",
                    "minimum": 1,
                    "description": "Minimal base-pair span for ViennaRNA in rf-fold (`-vms`)."
                },
                "rffold_only_common": {
                    "type": "integer",
                    "minimum": 1,
                    "description": "Only fold transcripts covered in at least this number of XML experiments (`-oc`)."
                },
                "rffold_fold_constraint_file": {
                    "type": "string",
                    "format": "file-path",
                    "exists": true,
                    "description": "Constraint file for allowed base-pairing positions (`-fc`)."
                },
                "rffold_unpaired_constraint_file": {
                    "type": "string",
                    "format": "file-path",
                    "exists": true,
                    "description": "Constraint file for required unpaired positions (`-uc`)."
                },
                "rffold_dotplot": {
                    "type": "boolean",
                    "description": "Generate dot plots from rf-fold (`-d`).",
                    "default": true
                },
                "rffold_shannon_entropy": {
                    "type": "boolean",
                    "description": "Compute and report Shannon entropy in rf-fold (`-sh`).",
                    "default": true
                },
                "rffold_vienna_rnaplot": {
                    "type": "string",
                    "description": "Path to ViennaRNA `RNAplot` binary for SVG structure plots in rf-fold (`-vrp`).",
                    "default": "RNAplot"
                },
                "r2dt": {
                    "type": "boolean",
                    "description": "Generate template-based 2D RNA structure diagrams using R2DT, coloured by normalised SHAPE/chemical-probing reactivity.",
                    "default": true
                },
                "rfwiggle_report_zeroes": {
                    "type": "boolean",
                    "description": "Report positions with zero reactivity in the WIG/BigWig output (`-z`). Enabled by default so genome browsers receive complete tracks without gaps.",
                    "default": true
                },
                "rfwiggle_keep_bases": {
                    "type": "string",
                    "description": "Restrict WIG output to specific bases using an IUPAC code or combination (e.g. `AC` for DMS probing) (`-kb`). Default: all bases (`N`)."
                },
                "rnaframework_container": {
                    "type": "string",
                    "description": "Container image used for local RNAframework modules.",
                    "default": "ghcr.io/vicbeg/rnaframework:2.9.6-r5-runtime"
                },
                "rnaframework_r_path": {
                    "type": "string",
                    "description": "Path to R executable used by rf-count, rf-norm, and rf-fold plotting.",
                    "default": "/usr/bin/R"
                }
            }
        },
        "institutional_config_options": {
            "title": "Institutional config options",
            "type": "object",
            "fa_icon": "fas fa-university",
            "description": "Parameters used to describe centralised config profiles. These should not be edited.",
            "help_text": "The centralised nf-core configuration profiles use a handful of pipeline parameters to describe themselves. This information is then printed to the Nextflow log when you run a pipeline. You should not need to change these values when you run a pipeline.",
            "properties": {
                "custom_config_version": {
                    "type": "string",
                    "description": "Git commit id for Institutional configs.",
                    "default": "master",
                    "hidden": true,
                    "fa_icon": "fas fa-users-cog"
                },
                "custom_config_base": {
                    "type": "string",
                    "description": "Base directory for Institutional configs.",
                    "default": "https://raw.githubusercontent.com/nf-core/configs/master",
                    "hidden": true,
                    "help_text": "If you're running offline, Nextflow will not be able to fetch the institutional config files from the internet. If you don't need them, then this is not a problem. If you do need them, you should download the files from the repo and tell Nextflow where to find them with this parameter.",
                    "fa_icon": "fas fa-users-cog"
                },
                "config_profile_name": {
                    "type": "string",
                    "description": "Institutional config name.",
                    "hidden": true,
                    "fa_icon": "fas fa-users-cog"
                },
                "config_profile_description": {
                    "type": "string",
                    "description": "Institutional config description.",
                    "hidden": true,
                    "fa_icon": "fas fa-users-cog"
                },
                "config_profile_contact": {
                    "type": "string",
                    "description": "Institutional config contact information.",
                    "hidden": true,
                    "fa_icon": "fas fa-users-cog"
                },
                "config_profile_url": {
                    "type": "string",
                    "description": "Institutional config URL link.",
                    "hidden": true,
                    "fa_icon": "fas fa-users-cog"
                }
            }
        },
        "generic_options": {
            "title": "Generic options",
            "type": "object",
            "fa_icon": "fas fa-file-import",
            "description": "Less common options for the pipeline, typically set in a config file.",
            "help_text": "These options are common to all nf-core pipelines and allow you to customise some of the core preferences for how the pipeline runs.\n\nTypically these options would be set in a Nextflow config file loaded for all pipeline runs, such as `~/.nextflow/config`.",
            "properties": {
                "version": {
                    "type": "boolean",
                    "description": "Display version and exit.",
                    "fa_icon": "fas fa-question-circle",
                    "hidden": true
                },
                "publish_dir_mode": {
                    "type": "string",
                    "default": "copy",
                    "description": "Method used to save pipeline results to output directory.",
                    "help_text": "The Nextflow `publishDir` option specifies which intermediate files should be saved to the output directory. This option tells the pipeline what method should be used to move these files. See [Nextflow docs](https://www.nextflow.io/docs/latest/process.html#publishdir) for details.",
                    "fa_icon": "fas fa-copy",
                    "enum": ["symlink", "rellink", "link", "copy", "copyNoFollow", "move"],
                    "hidden": true
                },
                "email_on_fail": {
                    "type": "string",
                    "description": "Email address for completion summary, only when pipeline fails.",
                    "fa_icon": "fas fa-exclamation-triangle",
                    "pattern": "^([a-zA-Z0-9_\\-\\.]+)@([a-zA-Z0-9_\\-\\.]+)\\.([a-zA-Z]{2,5})$",
                    "help_text": "An email address to send a summary email to when the pipeline is completed - ONLY sent if the pipeline does not exit successfully.",
                    "hidden": true
                },
                "plaintext_email": {
                    "type": "boolean",
                    "description": "Send plain-text email instead of HTML.",
                    "fa_icon": "fas fa-remove-format",
                    "hidden": true
                },
                "max_multiqc_email_size": {
                    "type": "string",
                    "description": "File size limit when attaching MultiQC reports to summary emails.",
                    "pattern": "^\\d+(\\.\\d+)?\\.?\\s*(K|M|G|T)?B$",
                    "default": "25.MB",
                    "fa_icon": "fas fa-file-upload",
                    "hidden": true
                },
                "monochrome_logs": {
                    "type": "boolean",
                    "description": "Do not use coloured log outputs.",
                    "fa_icon": "fas fa-palette",
                    "hidden": true
                },
                "hook_url": {
                    "type": "string",
                    "description": "Incoming hook URL for messaging service",
                    "fa_icon": "fas fa-people-group",
                    "help_text": "Incoming hook URL for messaging service. Currently, MS Teams and Slack are supported.",
                    "hidden": true
                },
                "multiqc_config": {
                    "type": "string",
                    "format": "file-path",
                    "description": "Custom config file to supply to MultiQC.",
                    "fa_icon": "fas fa-cog",
                    "hidden": true
                },
                "multiqc_logo": {
                    "type": "string",
                    "description": "Custom logo file to supply to MultiQC. File name must also be set in the MultiQC config file",
                    "fa_icon": "fas fa-image",
                    "hidden": true
                },
                "multiqc_methods_description": {
                    "type": "string",
                    "description": "Custom MultiQC yaml file containing HTML including a methods description.",
                    "fa_icon": "fas fa-cog"
                },
                "validate_params": {
                    "type": "boolean",
                    "description": "Boolean whether to validate parameters against the schema at runtime",
                    "default": true,
                    "fa_icon": "fas fa-check-square",
                    "hidden": true
                },
                "pipelines_testdata_base_path": {
                    "type": "string",
                    "fa_icon": "far fa-check-circle",
                    "description": "Base URL or local path to location of pipeline test dataset files",
                    "default": "https://raw.githubusercontent.com/nf-core/test-datasets/",
                    "hidden": true
                },
                "trace_report_suffix": {
                    "type": "string",
                    "fa_icon": "far calendar",
                    "description": "Suffix to add to the trace report filename. Default is the date and time in the format yyyy-MM-dd_HH-mm-ss.",
                    "hidden": true
                },
                "help": {
                    "type": ["boolean", "string"],
                    "description": "Display the help message."
                },
                "help_full": {
                    "type": "boolean",
                    "description": "Display the full detailed help message."
                },
                "show_hidden": {
                    "type": "boolean",
                    "description": "Display hidden parameters in the help message (only works when --help or --help_full are provided)."
                }
            }
        }
    },
    "allOf": [
        {
            "$ref": "#/$defs/input_output_options"
        },
        {
            "$ref": "#/$defs/reference_genome_options"
        },
        {
            "$ref": "#/$defs/sample_metadata_options"
        },
        {
            "$ref": "#/$defs/read_trimming_options"
        },
        {
            "$ref": "#/$defs/alignment_options"
        },
        {
            "$ref": "#/$defs/rnaframework_options"
        },
        {
            "$ref": "#/$defs/institutional_config_options"
        },
        {
            "$ref": "#/$defs/generic_options"
        }
    ]
}
